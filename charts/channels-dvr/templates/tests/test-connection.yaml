apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "channels-dvr.fullname" . }}-test-connection"
  labels:
    {{- include "channels-dvr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "channels-dvr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
